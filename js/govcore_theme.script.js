/**
 * @file
 * Behaviors for the govcore_theme theme.
 */

(function($, _, Drupal, drupalSettings) {
  Drupal.behaviors.govcore_theme = {
    attach: function(context) {
      // govcore_theme admin JavaScript behaviors goes here.
    }
  };
})(window.jQuery, window._, window.Drupal, window.drupalSettings);
